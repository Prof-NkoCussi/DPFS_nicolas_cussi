<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('../partials/head') %>
  <link rel="stylesheet" href="/styles/forms.css" />
</head>
<body>

  <%- include('../partials/header') %>


  <!-- ================================================
       BREADCRUMB
  ================================================ -->
  <div class="breadcrumb-wrapper">
    <div class="container">
      <nav class="breadcrumb">
        <a href="/">Inicio</a>
        <span class="breadcrumb-sep">›</span>
        <a href="/products">Productos</a>
        <span class="breadcrumb-sep">›</span>
        <a href="/products/<%= product.id %>"><%= product.name %></a>
        <span class="breadcrumb-sep">›</span>
        <span class="breadcrumb-current">Editar producto</span>
      </nav>
    </div>
  </div>


  <!-- ================================================
       FORMULARIO EDITAR PRODUCTO
  ================================================ -->
  <section class="auth-section">
    <div class="container">

      <div class="auth-card">

        <!-- Encabezado -->
        <div class="auth-header">
          <div class="auth-logo">✏️</div>
          <h1 class="auth-title">Editar producto</h1>
          <p class="auth-subtitle">Modificá los datos del producto y guardá los cambios</p>
        </div>

        <!-- Formulario -->
        <form class="auth-form" action="/products/<%= product.id %>?_method=PUT" method="POST" id="editForm" novalidate>

          <!-- Nombre -->
          <div class="form-group">
            <label class="form-label" for="name">Nombre del producto</label>
            <input
              type="text"
              id="name"
              name="name"
              class="form-input"
              placeholder="Ej: Guitarra Eléctrica Pro"
              value="<%= product.name %>"
            />
            <span class="form-error" id="nameError"></span>
          </div>

          <!-- Precio -->
          <div class="form-group">
            <label class="form-label" for="price">Precio ($)</label>
            <input
              type="number"
              id="price"
              name="price"
              class="form-input"
              placeholder="Ej: 89999"
              min="0"
              value="<%= product.price %>"
            />
            <span class="form-error" id="priceError"></span>
          </div>

          <!-- Categoría -->
          <div class="form-group">
            <label class="form-label" for="category">Categoría</label>
            <select id="category" name="category" class="form-input">
              <option value="" disabled>Seleccioná una categoría</option>
              <option value="guitarras"  <%= product.category === 'guitarras'  ? 'selected' : '' %>>Guitarras</option>
              <option value="teclados"   <%= product.category === 'teclados'   ? 'selected' : '' %>>Teclados</option>
              <option value="baterias"   <%= product.category === 'baterias'   ? 'selected' : '' %>>Baterías</option>
              <option value="microfonos" <%= product.category === 'microfonos' ? 'selected' : '' %>>Micrófonos</option>
              <option value="vientos"    <%= product.category === 'vientos'    ? 'selected' : '' %>>Vientos</option>
              <option value="accesorios" <%= product.category === 'accesorios' ? 'selected' : '' %>>Accesorios</option>
            </select>
            <span class="form-error" id="categoryError"></span>
          </div>

          <!-- Descripción -->
          <div class="form-group">
            <label class="form-label" for="description">Descripción</label>
            <textarea
              id="description"
              name="description"
              class="form-input"
              placeholder="Describí las características del producto..."
              rows="4"
            ><%= product.description %></textarea>
            <span class="form-error" id="descriptionError"></span>
          </div>

          <!-- Imagen -->
          <div class="form-group">
            <label class="form-label" for="image">Imagen (nombre del archivo)</label>
            <input
              type="text"
              id="image"
              name="image"
              class="form-input"
              placeholder="Ej: guitarra-001.png"
              value="<%= product.image %>"
            />
            <span class="form-hint">El archivo debe estar en la carpeta public/images/products/</span>
            <span class="form-error" id="imageError"></span>
          </div>

          <!-- Colores -->
          <div class="form-group">
            <label class="form-label" for="colors">Colores disponibles</label>
            <input
              type="text"
              id="colors"
              name="colors"
              class="form-input"
              placeholder="Ej: negro, rojo, azul"
              value="<%= product.colors ? product.colors.join(', ') : '' %>"
            />
            <span class="form-hint">Separalos con coma</span>
          </div>

          <!-- Botones -->
          <div class="form-actions">
            <a href="/products/<%= product.id %>" class="btn btn-secondary btn-lg">
              Cancelar
            </a>
            <button type="submit" class="btn btn-primary btn-lg">
              Guardar cambios
            </button>
          </div>

          <!-- Mensaje de error general -->
          <div class="form-alert error" id="formError" style="display:none;">
            ⚠️ Por favor corregí los errores antes de continuar.
          </div>

          <!-- Mensaje de éxito -->
          <div class="form-alert success" id="formSuccess" style="display:none;">
            ✅ ¡Producto actualizado con éxito! Redirigiendo...
          </div>

        </form>

      </div>
    </div>
  </section>


  <%- include('../partials/footer') %>


  <!-- ================================================
       JS — Validación básica
  ================================================ -->
  <script>
    document.getElementById('editForm').addEventListener('submit', function(e) {

      let valid = true;

      const fields = [
        { id: 'name',        errorId: 'nameError',        msg: 'El nombre es obligatorio.' },
        { id: 'price',       errorId: 'priceError',       msg: 'El precio es obligatorio.' },
        { id: 'category',    errorId: 'categoryError',    msg: 'Seleccioná una categoría.' },
        { id: 'description', errorId: 'descriptionError', msg: 'La descripción es obligatoria.' },
        { id: 'image',       errorId: 'imageError',       msg: 'El nombre de imagen es obligatorio.' },
      ];

      // Limpiar errores previos
      fields.forEach(f => {
        document.getElementById(f.id).classList.remove('error');
        document.getElementById(f.errorId).textContent = '';
      });
      document.getElementById('formError').style.display   = 'none';
      document.getElementById('formSuccess').style.display = 'none';

      // Validar campos vacíos
      fields.forEach(f => {
        const input = document.getElementById(f.id);
        if (!input.value.trim()) {
          input.classList.add('error');
          document.getElementById(f.errorId).textContent = f.msg;
          valid = false;
        }
      });

      if (!valid) {
        e.preventDefault();
        document.getElementById('formError').style.display = 'block';
      }
    });
  </script>

</body>
</html>
